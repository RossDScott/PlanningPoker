# Claude Instructions

## Versioning

The app version is defined in `PlanningPoker.Client/Features/AppVersion.cs`.

Increment the **patch** version number on each instruction (e.g. `1.0.0` → `1.0.1` → `1.0.2`).
